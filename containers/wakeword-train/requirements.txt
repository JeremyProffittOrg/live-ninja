# Wake-word training container dependencies — linux/arm64, Python 3.11.
#
# Every pin below was verified (2026-07) to publish a prebuilt manylinux aarch64
# cp311 wheel on PyPI, so the image needs NO compiler toolchain:
#   torch-2.3.1-cp311-cp311-manylinux2014_aarch64.whl
#   torchaudio-2.3.1-cp311-cp311-manylinux2014_aarch64.whl
#   onnxruntime-1.18.1-cp311-cp311-manylinux_2_27_aarch64.manylinux_2_28_aarch64.whl
#   onnx-1.16.1-cp311-cp311-manylinux_2_17_aarch64.manylinux2014_aarch64.whl
#   piper_phonemize-1.1.0-cp311-cp311-manylinux_2_28_aarch64.whl
#   webrtcvad_wheels-2.0.14-cp311-cp311-manylinux_2_17_aarch64.manylinux2014_aarch64.whl
#
# numpy<2 is required by piper-sample-generator v2.0.0 and matches the
# torch 2.3.1 / onnxruntime 1.18.1 ABI expectations.
numpy==1.26.4
scipy==1.13.1

# TTS synthesis (piper-sample-generator v2.0.0 runtime deps; its requirements.txt
# lists audiomentations + webrtcvad too — audiomentations is only used by its
# augment.py, which we do not import; webrtcvad has no aarch64 wheel, so we use
# the wheel-publishing fork webrtcvad-wheels, which installs the same
# `webrtcvad` module name).
torch==2.3.1
torchaudio==2.3.1
piper-phonemize==1.1.0
webrtcvad-wheels==2.0.14

# Feature extraction + export/quantization
onnx==1.16.1
onnxruntime==1.18.1

# openwakeword==0.6.0 is installed with --no-deps in the Dockerfile (its metadata
# hard-requires tflite-runtime on Linux, which we neither need — train.py forces
# AudioFeatures(inference_framework="onnx") — nor want in the image). These are
# its actual import-time deps:
tqdm==4.66.4
requests==2.32.3
scikit-learn==1.5.1

# S3 upload (credentials come from the Batch task role — never baked in)
boto3==1.34.144
