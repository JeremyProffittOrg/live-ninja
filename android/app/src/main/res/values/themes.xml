<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns all in-app theming; this platform theme only styles the
         window before the first frame. -->
    <style name="Theme.LiveNinja" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
