<?xml version="1.0" encoding="utf-8"?>
<!--
    WS-5 M24.2 instrumented-test manifest. Merged into the test APK only (never the app's
    own debug/release APK). Declares TestHarnessActivity, the @AndroidEntryPoint host used to
    exercise screens that normally sit behind LiveNinjaRoot's onboarding/sign-in gate (e.g.
    ConversationScreen) without a live account.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity
            android:name=".TestHarnessActivity"
            android:exported="false" />
    </application>
</manifest>
